# Claude Code Instructions

## Workflow

- Always create a feature branch before starting implementation work
- Always update `CAPABILITIES.md` before writing implementation code
- Backlog and feature tracking lives in GitHub Issues, not a markdown file — check `gh issue list` for pending work. Reference the issue in the PR body (e.g. `Closes #N`) so it closes automatically on merge.
